Search the memoire causal graph for: $ARGUMENTS

Call the `search` MCP tool (memoire-ai server) with query="$ARGUMENTS" and display the matching entities and relationships with their rationales and confidence scores.
